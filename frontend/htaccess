Options -Indexes
RewriteEngine On

# Serve real files and directories as-is
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Route /blog/<slug>/ to blog-single.html
RewriteRule ^blog/([a-z0-9-]+)/?$ /blog-single.html [L]
